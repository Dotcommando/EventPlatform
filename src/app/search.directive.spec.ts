import { AppSearchDirective } from './app-search.directive';

describe('AppSearchDirective', () => {
  it('should create an instance', () => {
    const directive = new AppSearchDirective();
    expect(directive).toBeTruthy();
  });
});
